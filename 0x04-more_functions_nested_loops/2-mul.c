#include "main.h"

/**
  *mul - multiplication for two integers
  *
  *@a: first integer
  *@b: second integer
  *Return: mult
  */

int mul(int a, int b)
{
	int mult;

	mult = (a * b);

	return (mult);
}
